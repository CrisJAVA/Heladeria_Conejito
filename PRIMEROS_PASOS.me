╔══════════════════════════════════════════════════════╗
║     HELADERÍA CONEJITO - GUÍA DE PRIMEROS PASOS      ║
╚══════════════════════════════════════════════════════╝


1. REQUISITOS
   ──────────────────────────────────────────────────
   • Node.js 18+         ─→  node --version
   • Java 21+            ─→  java --version
   • Maven (wrapper incluido, no necesita instalación)
   • PostgreSQL 15+      ─→  psql --version


2. CLONAR EL PROYECTO
   ──────────────────────────────────────────────────
   git clone <url-del-repositorio>
   cd Heladeria_Conejito


3. CONFIGURAR BASE DE DATOS
   ──────────────────────────────────────────────────
   a) Asegúrate de tener PostgreSQL corriendo.

   b) Crea la base de datos:
      psql -U postgres -c "CREATE DATABASE heladeria_conejito;"

   c) Carga el esquema y datos iniciales:
      psql -U postgres -d heladeria_conejito -f database/schema.sql

   d) Si tu contraseña de PostgreSQL es distinta a "postgres",
      edita backend/backend/src/main/resources/application.properties
      y cambia: spring.datasource.password=tu_password


4. LEVANTAR EL BACKEND (Spring Boot)
   ──────────────────────────────────────────────────
   cd backend/backend
   .\mvnw.cmd spring-boot:run

   • El servidor inicia en: http://localhost:8080
   • Endpoints disponibles en: http://localhost:8080/api/productos


5. LEVANTAR EL FRONTEND (React + Vite)
   ──────────────────────────────────────────────────
   cd frontend
   npm install        (solo la primera vez)
   npm run dev

   • El servidor inicia en: http://localhost:5173


6. ESTRUCTURA DEL PROYECTO
   ──────────────────────────────────────────────────
   Heladeria_Conejito/
   ├── database/           ← Esquema SQL (PostgreSQL)
   ├── backend/
   │   └── backend/        ← Spring Boot (Java 21)
   │       └── src/main/
   │           ├── java/.../controller/
   │           ├── java/.../service/
   │           ├── java/.../repository/
   │           ├── java/.../model/
   │           ├── java/.../dto/
   │           └── resources/
   │               └── application.properties
   └── frontend/           ← React + Vite + Tailwind
       └── src/
           ├── pages/
           ├── app/components/
           ├── context/
           └── ...


7. RUTAS DEL FRONTEND
   ──────────────────────────────────────────────────
   /                → Landing page
   /menu            → Menú interactivo
   /carrito         → Carrito de compras
   /login           → Inicio de sesión
   /contacto        → Contacto y ubicación
   /promociones     → Promociones activas
   /fidelizacion    → Programa de fidelización
   /editar-perfil   → Perfil de usuario
   /mis-pedidos     → Historial de pedidos
   /admin/*         → Panel administrativo


8. COMANDOS ÚTILES
   ──────────────────────────────────────────────────
   Backend:
     .\mvnw.cmd compile          → Compilar
     .\mvnw.cmd test             → Ejecutar tests
     .\mvnw.cmd clean            → Limpiar build

   Frontend:
     npm run dev                 → Desarrollo
     npm run build               → Build producción
